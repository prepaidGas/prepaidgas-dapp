import { theme } from './theme/themeVariables';

const config = {
  menuCollapse: false,
  topMenu: false,
  rtl: false,
  mainTemplate: 'lightMode',
  loggedIn: false,
  theme,
};

export default config;
